# Base URL for the backend REST API (no trailing slash), e.g. http://localhost:3001
REACT_APP_API_BASE_URL=

# Optional: explicit SignalR hub URL if different from API base URL
# If not set, the app will attempt `${REACT_APP_API_BASE_URL}/hubs/chat`
REACT_APP_SIGNALR_HUB_URL=
